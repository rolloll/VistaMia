<Window x:Class="ImageViewer.BatchRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Vista Mia" Height="600" Width="600"
        MinHeight="440" MinWidth="480"
        Icon="pack://application:,,,/Resources/icon.ico"
        WindowStartupLocation="CenterOwner">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
                <Image Source="pack://application:,,,/Resources/icon.ico" Width="16" Height="16"/>
                <TextBlock Text="폴더/파일 일괄 이름 변경" Margin="10,0,0,0" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="MinimizeButton" Content="&#x2500;" Style="{StaticResource CaptionButtonStyle}" Click="MinimizeButton_Click"/>
                <Button x:Name="CloseButton" Content="&#x2715;" Style="{StaticResource CloseCaptionButtonStyle}" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20,16,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="FolderPathText" Grid.Row="0" FontSize="11" Foreground="{StaticResource TextMutedBrush}"
                       TextTrimming="CharacterEllipsis" Margin="0,0,0,14"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,14">
                <TextBlock Text="대상" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextFaintBrush}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                <RadioButton x:Name="TargetFileRadio" GroupName="Target" Content="파일 (이 폴더의 이미지)" IsChecked="True" Margin="0,0,16,0" Checked="Target_Checked"/>
                <RadioButton x:Name="TargetFolderRadio" GroupName="Target" Content="하위 폴더" Checked="Target_Checked"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="이름 형식 — {name} 원본이름, {n} 순번, {n:3} 3자리 순번, {ext} 확장자(파일)"
                               FontSize="10" Foreground="{StaticResource TextFaintBrush}" Margin="0,0,0,6" TextWrapping="Wrap"/>
                    <TextBox x:Name="TemplateTextBox" Text="{}{name}" TextChanged="Template_Changed"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="시작 번호" FontSize="10" Foreground="{StaticResource TextFaintBrush}" Margin="0,0,0,6"/>
                    <TextBox x:Name="StartNumberTextBox" Text="1" TextChanged="Template_Changed"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="3" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}">
                <DataGrid x:Name="PreviewGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="기존 이름" Binding="{Binding OriginalName}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="새 이름 (직접 수정 가능)" Binding="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Grid Grid.Row="4" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="CountText" Grid.Column="0" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                <Button x:Name="ApplyButton" Grid.Column="1" Content="적용" Style="{StaticResource PrimaryButton}" Click="ApplyButton_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
